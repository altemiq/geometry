<Project>
 
  <Import Project="Sdk.props" Sdk="Microsoft.NET.Sdk" />

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <MinClientVersion>3.6</MinClientVersion>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <IncludeSymbols>false</IncludeSymbols>
    <DisableImplicitFrameworkReferences>true</DisableImplicitFrameworkReferences>
    <DisableSemanticVersioning>true</DisableSemanticVersioning>
    <GetVersionFrom>..\Data.Spatialite.Core\Data.Spatialite.Core.csproj</GetVersionFrom>
  </PropertyGroup>

  <PropertyGroup Label="Packaging">
    <Description>$(RootNamespace) is a lightweight ADO.NET provider for SpatiaLite.</Description>
    <PackageTags>$(PackageTags) Data SQLite SpatiaLite</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.Sqlite" VersionOverride="$(VerMicrosoft_Data_Sqlite)" />
    <PackageReference Include="SpatiaLite" VersionOverride="5.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="$(GetVersionFrom)" />
  </ItemGroup>

  <ItemGroup>
    <None Include="lib\**\*">
      <Pack>True</Pack>
      <PackagePath>lib</PackagePath>
    </None>
  </ItemGroup>

  <Import Project="Sdk.targets" Sdk="Microsoft.NET.Sdk" />

  <Target Name="Compile" />
  <Target Name="CopyFilesToOutputDirectory" />

</Project>
